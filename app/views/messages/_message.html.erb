<%# メッセージ部分のテンプレート %>

<div class="message">
  <div class="upper-message">
    <div class="message-user">
      <!-- 投稿したユーザー名情報を出力する -->
      <%= message.user.name %>
    </div>
    <div class="message-date">
      <!-- 投稿した時刻を出力する -->
      <%= l message.created_at %>
      <!-- lメソッド 指定した現地時間にする。日本時間の指定はconfig/application.rb と config/locales/ja.yml で行われている -->
    </div>
  </div>
  <div class="lower-message">
    <div class="message-content">
      <!-- 投稿したメッセージ内容を記述する -->
      <%= message.content %>
    </div>
    <%= image_tag message.image.variant(resize: '500x500'),class: 'message-image' if message.image.attached? %>
    <%# image_tag モデル.画像ファイル  はrailsのヘルパーメソッド。複雑なRailsのディレクトリパスを指定しなくてもモデルから画像ファイルを呼び出して引数に記述するだけで画像を表示するimg要素を生成する %>
    <%# モデル.ファイル名.variant(resize: '幅x高さ')  保存されている画像データを指定されたサイズに変換しその画像を返すメソッド %>
    <%# attached?   レコードにファイルが添付されているかどうかで、trueかfalseを返すメソッド。上記の用事記述することで画像が存在する場合のみimage_tagが読み込まれる %>

  </div>
</div>

